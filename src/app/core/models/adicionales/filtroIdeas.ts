
export interface FiltroIdeas {
    state?: string, // puede ser 'CREADA' || 'ENVIADA' || 'CALIFICADA' 
    institucionId?: string,
    number?: string,
    unitExecute?: string,
    author?: string,
}
